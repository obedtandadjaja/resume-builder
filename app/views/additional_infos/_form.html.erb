<%= form_for(@additional_info) do |f| %>
  <% if @additional_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@additional_info.errors.count, "error") %> prohibited this additional_info from being saved:</h2>

      <ul>
      <% @additional_info.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :resume_id %><br>
    <%= f.text_field :resume_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
